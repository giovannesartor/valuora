[build]
builder = "nixpacks"

[deploy]
startCommand = "python init_db.py && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
# redeploy: 2026-02-25

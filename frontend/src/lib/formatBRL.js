/**
 * formatBRL — backward-compatible shim for Valuora.
 * Formats a number as USD currency (Valuora is USD-based).
 */
export { default, formatCurrency as formatBRL, formatCurrency as formatUSD } from './formatCurrency';

feat: implement all 10 platform improvements

#1 R2 storage: storage_service.py with boto3 R2/S3 upload + local fallback;
   analysis.py _save_logo() delegates to service; config.py R2_* env vars added

#2 Coupon table: Coupon model in models.py; migration 008_add_coupons.py;
   payments.py replaces hardcoded PRIMEIRA with DB lookup + max_uses/expiry;
   coupon_code field added to Payment

#3 PyJWT: python-jose replaced with PyJWT==2.10.1; security.py import updated

#4 Rate limits: login bucket capped at 5 req/60s (was 20)

#5 Simulation history: GET /analyses/{id}/simulations endpoint;
   SimulatorPage.jsx shows history table after results

#6 Coupon field: AnalysisPage.jsx coupon input state + inline error + sends to API

#7 SSE: GET /payments/{id}/stream EventSourceResponse (sse-starlette added)

#8 Revenue metrics: GET /admin/plan-breakdown (revenue + count + avg ticket);
   AdminDashboardPage.jsx fetches and displays plan breakdown cards

#9 Admin coupon CRUD: routes in admin.py; AdminCouponsPage.jsx with form + table;
   App.jsx route /admin/cupons; AdminSidebar Cupons nav item

#10 Split analysis.py: simulate + list_simulations extracted to routes/simulation.py
    (registered in main.py before analysis router); analysis.py -100 lines
